# W1 · Optimize the Individual AI Workflow — Activity

**Walkthrough:** [`../../../curriculum/workshops/W1/README.md`](../../../curriculum/workshops/W1/README.md)
**Reference example:** [`../../../reference-project/fired-up-pizza/workflow-card.md`](../../../reference-project/fired-up-pizza/workflow-card.md)

## Deliverable

A single file in this folder:

* `workflow-card.md` — one page describing how *you*, personally, drive a CLI coding agent on a single feature end-to-end. Sections: situation, rules you rely on, interventions, anti-patterns, exit criteria.

Use the reference card as the target shape. Don't try to outline-then-fill — write inline, one section at a time, ~5 min each.

## Workspace wiring

W1 is a concept session — no pack is installed and `../../../my-factory/city.toml` is not touched.

## Exit criteria

* [ ] `workflow-card.md` exists in this folder and is roughly the length of the reference.
* [ ] At least one rule on the card is concrete enough that you could paste it into your relevant agent instruction file (ex. `CLAUDE.md`) verbatim.

## Skipped this session?

Nothing downstream depends on the card existing — but L1 asks you to pull rules from it into the relevant agent instruction files (ex. `CLAUDE.md`). If you skip W1, draft rules directly in L1 using the reference card as a starting point.
