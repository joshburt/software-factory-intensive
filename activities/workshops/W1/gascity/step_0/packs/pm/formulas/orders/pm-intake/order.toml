[order]
description = "Wake the pm when any bead is labelled needs-pm"
formula = "mol-plan-breakdown"
gate = "condition"
check = "[ -n \"$GC_RIG\" ] && gc bd --rig \"$GC_RIG\" list --label=needs-pm --status=open --no-assignee --json 2>/dev/null | jq -e 'length > 0' > /dev/null 2>&1"
pool = "pm"
