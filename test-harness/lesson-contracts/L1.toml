[lesson]
id = "L1"
pack_dir = ""
entry_formula = ""
entry_agent = ""
roles = []
docs = [
  "activities/labs/L1/README.md",
  "curriculum/labs/L1/README.md",
]

# L1 has no formula steps — it is a setup-only lesson.
# The walkthrough validates: CLAUDE.md exists, PROJECT_MANIFEST.md created,
# city.toml enables FormulaV2 and selects L2, gc register + rig add
# succeed, and project tests pass.
