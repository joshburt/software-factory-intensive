# Raw signals for 2026-04-18 (window: 24h — extended to "since last successful harvest")

**Note:** The rig is a workshop reference project. No production error tracker,
metrics backend, log aggregator, or support inbox is wired up. Where a signal
source is absent, the corresponding section records "no access" and the harvest
falls back to artifact-derived signals (bead state, retrospectives, feedback
loops, review reports, release gates).

Collection methods used:
- `bd list --status=open` and `bd list --status=closed --limit 30` against the
  fupr2 rig (fired-up-pizza-review2)
- `git log --oneline -30` on the fired-up-pizza working tree
- Inspection of `feedback-loops/*.md`
- Inspection of `docs/reviews/*.md` and `docs/releases/*.md`
- Inspection of `retrospective-card.md`, `factory-run-report.md`, `DECISIONS.md`

---

## Errors

no access — no error tracker (Sentry/Rollbar/etc.) wired into the rig. No
production crash reports, no uncaught-exception signatures.

## Metrics

no access — no metrics backend (Prometheus/Datadog/Grafana) exposed. The
docs/releases report informational values (build time 4.8s, bundle 141.27KB,
test coverage 87%) but there is no continuous SLO dashboard to burn against.

## Logs

no access — no centralized log aggregator. Local `npm run dev` logs are not
persisted across sessions.

## User feedback

Source 1: `feedback-loops/external-customer-points-discrepancy.md`
- 2026-04-16 · Maria C. · FUP-ORDER-8421 · UI showed stale balance on
  confirmation page (cache-miss on first render).
- Root cause: designer-spec omitted `revalidateOnMount: true`. Fixed on that
  bead, but not promoted to a cross-cutting rule. History table has exactly
  one row — if a second customer reports a stale-on-first-render defect on a
  different page, the pattern justifies a docs/designs/architecture rule.

Source 2: `feedback-loops/coder-missing-try-catch.md`
- 2026-04-16 · Order History · caught by rule, 0-min cost.
- Rule is holding — no new async-handler rejections this window.

Source 3: `feedback-loops/aggregate-vague-acceptance-criteria.md`
- Last trigger 2026-04-15 at 5/5. Rule was applied 2026-04-16. History table
  has no rows after 2026-04-16, so the counter is implicitly reset but not
  yet *verified* — need one more planner run to confirm the rule sticks.

## CI

no access — no CI service wired up (GitHub Actions / CircleCI etc.). `npm
test`, `npm run lint`, `npm run build` results appear only inside release-gate
reports. All reported values for both features (loyalty, order-history) are
green.

## Deps

no access — no dependency vulnerability scanner running (Dependabot, Snyk,
etc.). `package.json` present but not cross-checked against advisories this
cycle.

---

## Artifact-derived signals

### A1 · Two prompt edits from 2026-04-16 are logged only in DECISIONS.md

Source: `retrospective-card.md` "Question" section and `factory-run-report.md`
"Ad-Hoc Prompts Used" → "Feedback Rules Triggered".
- During the order-history capstone, two prompt edits landed:
  1. Architect: "List ≥3 storage or access-pattern alternatives when a
     scaling question exists; one-option ADRs are rejected."
  2. Builder: "Pagination helpers must accept `cursor` as `string | null`,
     not `number`. Never rely on sequential numeric offsets for ordered
     lists."
- Both live in `DECISIONS.md` entries but not in `feedback-loops/*.md`.
- Open question from retrospective: **what's the threshold for promoting a
  prompt edit to a reactive feedback loop?** One additional feature's worth of
  evidence? Three?

### A2 · Review-report "Low" findings not routed to any backlog

Source: `docs/reviews/loyalty-points-review.md` (3 Low), plus
`factory-run-report.md` reference to 2 Low findings on the order-history
review (JSDoc terseness, missing `aria-busy` during fetch).
- Five total Low-severity findings across the last two reviewed features,
  none tracked as beads. Each has an explicit "Recommendation: a future
  accessibility sweep" or "future Style task" that was never created.
- If these keep accumulating, they become the drift the retrospective warns
  about.

### A3 · Out-of-scope items from loyalty work package never tracked

Source: `docs/plans/loyalty-points-system.md` "Scope → OUT".
- Redemption flow (future work package) — no bead filed.
- Historical backfill for orders placed pre-feature — no bead filed.
- Admin UI for staff balance view — no bead filed.
- Anti-abuse (cancel-and-reorder) — acknowledged in ADR, no bead filed.
- Without a tracking bead, these will re-surface only when a customer or
  staff request triggers them — which defeats the point of scoping them out
  explicitly.

### A4 · Stale harvest cycle left mid-flight

Source: `bd show fupr2-vqt`.
- Previous harvest (2026-04-17) closed steps 1–3 with `close_reason`s
  referencing `docs/feedback/2026-04-17.md` and `.raw.md` — but those files
  do not exist on disk. Step 4 (file-beads) is still open.
- Either the files were deleted after the run, or the close reasons
  overstated what happened. Either way, the cycle did not produce upstream
  beads. This is a process-discipline gap, not a feature bug.

### A5 · Aspirational prompt lines not yet converted to imperatives

Source: `retrospective-card.md` "Change" section.
- The retrospective commits to "flag any line that doesn't name a specific
  number, file, or rejection condition" across all pack prompts — but there
  is no bead tracking the audit. Risk is the same lesson re-emerging on the
  next feature run.

### A6 · `src/main.tsx` is a placeholder, not a functioning app entry

Source: `src/main.tsx` (26 lines).
- App renders only a hero header and a "coming soon" paragraph. The features
  from `factory-run-report.md` (order-history page, loyalty balance) and the
  loyalty review report reference source files like
  `src/pages/OrderConfirmation.tsx` that do not exist in this tree.
- Read in context: the workshop/reference project ships with artifacts that
  describe features *as if* built, but the code tree under `src/` contains
  only the placeholder `main.tsx`. This is intentional for a workshop
  reference, but a new user following the artifacts will be confused about
  which files to expect.
