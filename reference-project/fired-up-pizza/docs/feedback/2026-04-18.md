# Feedback harvest 2026-04-18

**Window:** 24h (extended to "since last successful harvest on 2026-04-17
which did not file beads").
**Rig:** fired-up-pizza-review2 (prefix `fupr2`).
**Raw input:** [`2026-04-18.raw.md`](./2026-04-18.raw.md).

No error tracker, metrics, logs, or CI signals are accessible. All signals
are artifact-derived (beads, retrospectives, feedback loops, reviews, gates).

## Classification

| # | Type | Severity | Signal | Proposed action | Upstream agent |
|---|------|----------|--------|-----------------|-----------------|
| 1 | arch | p2 | Two prompt edits from 2026-04-16 (Architect ≥3-alternatives rule; Builder cursor-typing rule) live only in `DECISIONS.md`; no threshold decides when prompt edits get promoted to `feedback-loops/*.md`. | Decide the promotion threshold for prompt edits (count-of-runs or criteria), then write an ADR documenting the rule. | architect |
| 2 | ux | p2 | Five Low-severity review findings (3 loyalty, 2 order-history: JSDoc terseness, missing `aria-live`/`aria-busy`, zero-value fields) have no backlog tracking — each review recommended "a future accessibility sweep" or "Style task" and none exists. | Create a single "accessibility + style sweep" work package covering the five findings, with concrete file:line evidence. | planner |
| 3 | docs | p2 | Loyalty work-package lists 4 out-of-scope items (redemption flow, historical backfill, admin UI, anti-abuse) as "future work package" but no tracking beads exist. | File one bead per out-of-scope item with a one-sentence scope note, label as `needs-plan`, so they surface in `bd ready` when priority changes. | planner |
| 4 | docs | p1 | Previous harvest (fupr2-vqt) closed steps 1–3 claiming files in `docs/feedback/` exist, but the files are absent. Step 4 (file-beads) is still open and no beads were filed from the claimed drafts. | Close `fupr2-vqt` and `fupr2-vqt.4` with an explicit note that the cycle did not produce beads and that 2026-04-18 supersedes it. (No upstream bead required — this is cleanup done in step 4 of *this* cycle.) | n/a — handled in file-beads step |
| 5 | arch | p2 | Retrospective commits to an audit of every pack prompt for aspirational lines (any line without a specific number, file, or rejection condition), but no tracking bead exists. | File an architect bead to run the audit across the 6 pack prompts (`packs/{planner,architect,designer,builder,reviewer,release-gate}/agents/*/prompt.template.md`) and produce a diff PR. | architect |
| 6 | ux | p2 | The external-loop history for customer-reported balance staleness has one row (Maria C., 2026-04-16). The fix (`revalidateOnMount: true`) was applied only to the confirmation page; no cross-page rule requires fresh-on-mount data for customer-visible balances. | Have the designer draft a spec rule requiring `revalidateOnMount: true` on any component showing customer-visible monetary or points state; cross-reference the loyalty confirmation fix. | designer |
| 7 | docs | p2 | `src/main.tsx` ships as a "coming soon" placeholder while review/gate artifacts reference files like `src/pages/OrderConfirmation.tsx` that don't exist in this tree. A new reader of the workshop reference can't tell which files are real vs. illustrative. | Add a one-paragraph note to `README.md` (or a new `docs/WORKSHOP_NOTES.md`) clarifying that artifact files reference *hypothetical* source paths used during factory runs; the actual repo ships only the placeholder `main.tsx`. | planner (docs work) |

No rows were judged *not actionable*. All 7 signals route to an upstream
agent; row 4 is handled in-cycle as part of closing the stale harvest.

## Proposed beads

### Draft 1 — Decide prompt-edit → feedback-loop promotion threshold

- **Title:** Define promotion threshold: `DECISIONS.md` prompt edit → `feedback-loops/*.md` reactive rule
- **Upstream label:** `needs-architecture`
- **Severity label:** `p2`
- **Body:**
  - **Evidence:** `retrospective-card.md` "Question" section (2026-04-16);
    `factory-run-report.md` "Ad-Hoc Prompts Used" → 2 edits pending
    promotion.
  - **Observed:** Two prompt edits (Architect ≥3-alternatives rule; Builder
    cursor-typing rule) exist only in `DECISIONS.md`. No written rule says
    when to copy them to `feedback-loops/`.
  - **Expected:** A documented threshold — e.g. "after the rule survives
    one additional feature without regression" or "after one other feature
    where the rule-would-have-caught a defect." The threshold should be
    observable from artifacts (git log on pack prompts + run reports).
  - **Suggested action:** Write ADR under `docs/architecture/` titled *"Promotion
    rules for reactive feedback loops"*. Include:
    1. Threshold criterion (with a counting rule).
    2. Evidence required to claim promotion.
    3. Mechanical step (write `feedback-loops/<slug>.md`, cross-link
       `DECISIONS.md` entry).
    4. Grandfathering for the two pending 2026-04-16 edits.

### Draft 2 — Accessibility + style sweep: 5 Low findings across loyalty + order-history

- **Title:** Accessibility + style sweep: resolve 5 Low findings from loyalty and order-history review reports
- **Upstream label:** `needs-plan`
- **Severity label:** `p2`
- **Body:**
  - **Evidence:**
    - `docs/reviews/loyalty-points-review.md` findings Low-1 (JSDoc terseness on `award`/`rollback`/`balance` in `src/db/pointsLedger.ts:32`), Low-2 (missing `aria-live` on `src/components/LoyaltyBalance.tsx:18`), Low-3 (`points_earned: 0` noise in `src/api/orders.ts:107`).
    - `factory-run-report.md` order-history row → 2 Low findings (JSDoc terseness, missing `aria-busy` during fetch).
  - **Observed:** Every review approved with Low findings explicitly marked
    "future sweep" — but no sweep bead exists, so the findings never land in
    `bd ready`.
  - **Expected:** One work package grouping the 5 findings, each with
    file:line evidence; planner decomposes into builder tasks.
  - **Suggested action:** Planner produces
    `docs/plans/a11y-and-style-sweep.md` with a checklist. ACs must be
    measurable — e.g. "`LoyaltyBalance` has `aria-live='polite'`" and
    "`OrderHistoryPage` sets `aria-busy` during fetch". Reviewer verifies
    against the file:line evidence above.

### Draft 3 — Track loyalty out-of-scope items as dormant beads

- **Title:** File one tracking bead per loyalty out-of-scope item (redemption, backfill, admin UI, anti-abuse)
- **Upstream label:** `needs-plan`
- **Severity label:** `p2`
- **Body:**
  - **Evidence:** `docs/plans/loyalty-points-system.md` "Scope → OUT"
    (4 items). No corresponding beads in current `bd list`.
  - **Observed:** Explicit scope-out decisions were made but no durable
    tracking exists. A future customer request or staff ask will re-surface
    them with lost context.
  - **Expected:** 4 dormant beads with titles like "Loyalty: redemption
    flow (deferred)", each priority `p2`, labelled `needs-plan` +
    `deferred`. Planner does not decompose them now; they surface in
    `bd ready` only when promoted.
  - **Suggested action:** Planner creates the 4 beads referencing the
    exact bullet in the loyalty work package. Keep them parked until a
    founder or external signal promotes one.

### Draft 4 — Audit pack prompts for aspirational lines

- **Title:** Audit all 6 pack prompts for aspirational (non-imperative) lines; convert to measurable rules
- **Upstream label:** `needs-architecture`
- **Severity label:** `p2`
- **Body:**
  - **Evidence:** `retrospective-card.md` "Change" section: "flag any line
    that doesn't name a specific number, file, or rejection condition".
  - **Observed:** The architect prompt already carried *"consider
    alternatives"* — an aspiration, not an imperative — and that was the
    vague-rule that failed the Architect sling 1 on order-history.
  - **Expected:** A sweep across `packs/{planner,architect,designer,builder,reviewer,release-gate}/agents/*/prompt.template.md`
    producing a diff PR that either (a) converts the aspiration into a
    measurable rule or (b) deletes it.
  - **Suggested action:** Architect reads each prompt, applies the rule
    from the retrospective, emits a single diff PR with a commit message
    referencing this bead. Quality gate: every line in the final diff
    names a specific number, file, or rejection condition.

### Draft 5 — Require `revalidateOnMount: true` on customer-facing balance UIs

- **Title:** Designer spec rule: customer-visible monetary/points components must set `revalidateOnMount: true`
- **Upstream label:** `needs-design`
- **Severity label:** `p2`
- **Body:**
  - **Evidence:** `feedback-loops/external-customer-points-discrepancy.md`
    (Maria C., FUP-ORDER-8421, 2026-04-16). Fix applied to confirmation
    page only; no cross-page rule exists.
  - **Observed:** A stale balance on first render is a cache-miss failure
    mode that generalizes beyond the confirmation page (order-history,
    loyalty dashboard when built, admin UI).
  - **Expected:** A rule in the designer pack prompt (or a designer-level
    style guide) that any component rendering customer-visible monetary or
    points state must fetch fresh on mount. Followed by a designer review
    of existing components for retroactive compliance.
  - **Suggested action:** Designer updates
    `packs/lessons/C1/agents/designer/prompt.template.md` with the
    imperative; additionally emits a sweep of currently-planned components
    that render monetary/points state and flags any without the rule.

### Draft 6 — Document that workshop-reference artifacts reference hypothetical source paths

- **Title:** README note: workshop-reference artifacts reference source files not present in this tree
- **Upstream label:** `needs-plan`
- **Severity label:** `p2`
- **Body:**
  - **Evidence:** `src/main.tsx` (26 lines, "coming soon" placeholder);
    `docs/reviews/loyalty-points-review.md` referencing
    `src/db/pointsLedger.ts`, `src/components/LoyaltyBalance.tsx`, etc.;
    `factory-run-report.md` referencing `src/pages/OrderHistoryPage.tsx`.
  - **Observed:** None of the referenced source files exist. A reader of
    the reference project has no signal that the artifacts describe
    hypothetical runs, not code shipped with the repo.
  - **Expected:** A single paragraph (README or new
    `docs/WORKSHOP_NOTES.md`) clarifying the relationship between
    artifacts and source tree.
  - **Suggested action:** Planner drafts a one-page doc-only work package:
    *"Clarify workshop-reference artifact-vs-source relationship."* No
    code changes; planner emits the paragraph, builder commits the file.
