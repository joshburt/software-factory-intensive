# workshop — Software Factory Intensive integrations pack.
#
# No agent. Pure infrastructure:
#   - 7 doctor checks for core tools + tracker/cloud/observability integrations
#   - 3 commands (status, setup, sync-all)
#   - 4 tracker-sync orders (github, gitlab, jira, linear)
#   - overlay/.claude/settings.json — MCP servers + permissions for
#     GitHub, Sentry, PostHog, Datadog, Grafana, Slack, Linear. Reaches
#     agent sessions via PackOverlayDirs when this pack is imported at
#     city scope (verified runtime path; see
#     internal/config/pack.go § ExpandPacks and
#     internal/runtime/tmux/adapter.go § session setup).

[pack]
name = "workshop"
schema = 2
version = "1.0.0"
